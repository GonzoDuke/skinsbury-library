node_modules/
.next/
out/
.env*.local
.env
.DS_Store
*.tsbuildinfo
next-env.d.ts
.vercel

# Local test photos at the repo root (never commit shelf photos).
# Leading slash anchors to root, so /public/some-asset.jpg etc. stays trackable.
/*.jpeg
/*.jpg

# Claude / IDE working files
.claude/
cc-update-batch.md
